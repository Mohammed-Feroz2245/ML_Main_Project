name: CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -f docker/Dockerfile.api -t course-completion-api .

      # Next steps (when AWS is ready):
      # - Login to ECR
      # - Push image
      # - Update ECS service
